/**
 *	Menu Setup
 *	If adding anything here, be sure to add it in
 *	the Menu Functions too.
 */

#include "Globals.h"

void MenuSetup()
{
	InitMenuSetup();
	
	if(InMenuLevel(0))
	{ 
		
		return;
	}
	
	if(InMenuLevel(1))
	{ 
		
		return;
	}
	
	if(InMenuLevel(2))
	{ 
		
		return;
	}
	
	if(InMenuLevel(3))
	{ 
		
		return;
	}
	
	if(InMenuLevel(4))
	{ 
		
		return;
	}
	
	if(InMenuLevel(5))
	{ 
		
		return;
	}
	
	//check
	print("ERROR: menu setup not ended");
}
		
		
		